# Company-Employee-Dataset-Cleaning
A complete Company Employee Dataset Cleaning project using Python and Pandas. The project identifies and handles missing values, inconsistent entries, incorrect data types, and duplicate records using data cleaning techniques such as isnull(), fillna(), median/mode imputation, text standardization, and drop_duplicates(). 
